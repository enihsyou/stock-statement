# stock-statement

使用 uv 管理、Rich 展示的证券流水分析工具，自动识别招商证券资金流水与东方财富证券交割单，并在报告开头显示平台名称。

```powershell
uv run stock-statement earnings "data/20260907 资金流水.txt"
uv run stock-statement earnings "data/Table_6188.txt"
uv run stock-statement earnings "data/第一段资金流水.txt" "data/第二段资金流水.txt" "data/Table_6188.txt"
```

支持多个文件及混合平台输入。同一平台的分段流水合并排序后核算，各平台分别维护持仓与移动加权成本，再按证券代码汇总收益、费用和剩余成本。每个平台的输入应属于同一个账户。

重叠导出按平台、日期、时间、流水号、业务、证券代码、数量、发生金额、成交金额、单价及费用识别重复；只去除跨文件重复，同一文件内相同记录的出现次数会保留，多个文件取最大出现次数。不同平台的流水不会互相去重。应输入同一类型的详细流水，不要混入按日归并的汇总记录。报告显示全部平台、文件数、去重后的流水数和重复条数；错误及待确认流水附来源文件与行号。

跨平台合计沿用各账户的托管成交金额，不自动配对抵消转移。两端估值不同时，托管净转入可能仍有差额；这份报告是各账户核算结果的合计，不追溯跨账户原始成本。

招商使用成交日期、业务名称和七项费用列；东方财富使用发生日期、发生时间、交易类别及其费用列。东方财富未单列的经手费、证管费、结算费显示为零，仅表示没有独立披露金额，其他费用不再拆分。优先按 GB18030
解码，失败后使用 UTF-8。按固定列宽解析空字段，按日期、发生时间（若有）、流水号排序；
流水号保留原文，纯数字按数值排序，字母数字混合编号按文本排序，同一时间数字编号排在文本编号前，最后以原始行号稳定排序。
东方财富逆回购购回使用交收日期核算，并排在交收日带时间的交易之前；其发生日期可能仍为拆出日，发生时间不用于购回排序。报告日期范围也使用这一核算日期。
证券以代码归组并使用最后一条记录中的名称。金额使用 Decimal。

平台差异由 `platforms.py` 中的适配器注册表集中定义，包括表头识别、字段映射、业务名称、交收日期、费用列、逆回购数量单位和展示说明。解析和核算通过统一接口使用这些规则，不按平台名称分支。

## 计算口径

- 现金净投入 = 银行转存 − 银行转取；证券转托不按现金计入。
- 账户净投入 = 现金净投入 + 托管净转入。托管净转入按成交金额计算，转入为正、转出为负；同一证券多笔转移累加为净额。
- 概览中的收益率 = 已实现净收益 ÷ 账户净投入；账户净投入为零时显示“收益率不可计算”。存在未识别业务或缺失托管金额时，收益率基于已知金额并同时标注收益不完整。
- 股票已实现收益使用移动加权成本：买入净支出进入持仓成本，卖出净收款
  减去相应数量的持仓成本。尚未卖出部分只显示历史成本，不计算浮动收益。
- 分红及补缴红利税按发生金额计入证券收益；资金利息单列并计入总收益。
- 托管转入使用成交金额作为本账户的新增持仓成本，单位成本为成交金额除以数量，不追溯其他账户的原始买入成本。
  托管转出按当日开盘价、转入按当日收盘价记录成交金额；两者之间因当天价格变化产生的差额，表示这段持有期间的浮动盈亏。托管金额不作为银行转账处理。
  成交金额、数量、单价优先保留原始字段，缺少其中一项时根据另外两项推算（除数不能为零）。招商托管记录使用成交价格乘以数量的绝对值补全金额；东方财富优先使用成交金额。不使用含手续费的发生金额代替成交金额。
  数据不足且无法推算托管金额时，相关收益、成本和托管净额显示为“—”，报告标记为不完整。逆回购价格为年化利率，不参与金额、单价、数量的乘除换算。
  东方财富同日、同证券、同数量的零金额撤指转出与指定入账逐条配对抵消；零数量零金额的指定交易及撤销指定不影响资产。
- 整份文件无有效托管转入转出业务时，概览隐藏托管项目，证券表隐藏托管净转入列；有转移但净额为零时仍保留。各表只在有备注内容时显示备注列。
- 逆回购拆出净支出扣除明列费用得到本金，拆出费用即时扣减收益。
  购回按平台流水数量还原本金：招商上海 204 系列每单位 1,000 元，深圳 1318 系列每单位 100 元；东方财富每单位 100 元。
  购回净收款减本金为净利息；未到期本金并入剩余成本，未到期原始数量并入剩余数量。
- 交收资金修正按同日、同金额与购回逐条匹配，匹配项单列，不重复计算收益。
  不匹配的调整作为待确认业务显示。
- 印花税、佣金、经手费、证管费、结算费、过户费和其他费用分别累计，
  与证券收益合并展示，按已实现净收益降序排列，无法确定收益的证券置后，末行显示合计。费用已经包含于净收付款，不再次扣减。
- 核算保留 Decimal 精度；展示金额逐证券四舍五入至分，再相加得到概览与合计，避免可见明细相加相差一分钱。零值不显示负号。数量合计仅为原始单位之和，不表示统一资产单位。
- 交易笔数为买入、卖出及逆回购拆出的流水记录数，不等同于委托单数。

不计算当前市值或总资产。完整账户收益需要完整交易流水及托管转入转出的成交金额。
无法解析的行或不足的持仓成本会停止报告，未知业务会列明并将报告标记为
不完整（退出码 1）；输入错误退出码 2。工具不会静默将未知业务认作银行转账。
